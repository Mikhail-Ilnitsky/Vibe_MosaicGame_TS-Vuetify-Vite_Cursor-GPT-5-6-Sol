# Игра-мозаика

Адаптивная браузерная игра, в которой изображение нужно собрать из перемешанных квадратных фрагментов. Это чистое SPA без бэкенда и базы данных.

**Демо:** [mikhail-ilnitsky.github.io/Vibe_MosaicGame_TS-Vuetify-Vite_Cursor-GPT-5-6-Sol](https://mikhail-ilnitsky.github.io/Vibe_MosaicGame_TS-Vuetify-Vite_Cursor-GPT-5-6-Sol/)

## Особенности

Собрано по подробному промпту с помощью **GPT-5.6-Sol-Medium**

Начальный промпт (с первого раза достаточно хорошо, дальше не правил):
- 15 запусков (автоматически) GPT-5.6-Sol-Medium = 1787 Ktoken

Всего в сумме: 1787 Ktoken = 1.8 Mtoken

## Возможности

- галерея из шести изображений и полная локализация на русском и английском;
- автоматический расчёт сеток сложности от 5 до 11 квадратов по меньшей стороне;
- управление «клик-клик» и перетаскивание на Pointer Events;
- адаптивное игровое поле, целиком помещающееся в доступную область экрана;
- счётчик ходов и просмотр оригинала во время игры;
- проверка решения, вспышка поля и анимация победы.

## Стек

| Технология | Версия |
| --- | --- |
| TypeScript | 5.9.3 |
| Vue | 3.5.42 |
| Vuetify | 3.13.4 |
| Vite | 8.3.0 |

Все Vue-компоненты используют Composition API и `<script setup lang="ts">`.

## Происхождение проекта

Исходная версия сгенерирована в Cursor по техническому заданию (см. [`prompts/INITIAL.md`](prompts/INITIAL.md)). Краткое описание архитектуры и ключевых решений находится в [`.cursor/CONTEXT.md`](.cursor/CONTEXT.md).

## Запуск

Требуется Node.js 22 или новее.

```bash
npm install
npm run dev
```

Другие команды:

```bash
npm run type-check  # проверка типов
npm run build       # production-сборка
npm run preview     # локальный просмотр production-сборки
```

Для публикации в **Settings → Pages** выберите источник сборки **GitHub Actions**. Workflow автоматически собирает и развёртывает приложение после отправки изменений в ветку `main`.

## Структура

```text
src/
├── composables/
│   └── useI18n.ts       # реактивная локализация RU/EN
├── data/
│   └── images.ts        # стабильный каталог изображений
├── plugins/
│   └── vuetify.ts       # тема и настройки Vuetify
├── styles/
│   └── main.css         # адаптивные стили и анимации
├── App.vue              # экраны и игровая логика
├── main.ts              # точка входа приложения
└── types.ts             # типы предметной области
```
